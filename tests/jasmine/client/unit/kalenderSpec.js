/**
 * Created by kristel on 13.10.14.
 */
/*describe('dateExists', function() {
    it("should return true if date exists", function() {
        var cursor = {
            count: function () {
                return 1;
            }
        };
        spyOn(Lectures, 'find').and.returnValue(cursor);
        expect(selectedDate.dateExists()).toBe(true);
    });
});*/