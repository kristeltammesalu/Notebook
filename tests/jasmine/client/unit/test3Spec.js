/**
 * Created by kristel on 9.12.14.
 */


/*describe('getLectureList', function () {
    it('should ask for the lectures in primarily in descending score order, then in alphabetical order and return them', function () {
        var result = {};
        spyOn(Lectures, 'find').and.returnValue(result);

        expect(lectures.getLectureList()).toBe(result);
        expect(Lectures.find.calls.argsFor(0)).toEqual([{}, {sort: {begintime: 1}}]);
    });
});*/